
Files in this directory are language independent templates.

Since openwebmail caches the content of template files in memory,
sharing files among all languages would reduce the memory used by templatecache.

